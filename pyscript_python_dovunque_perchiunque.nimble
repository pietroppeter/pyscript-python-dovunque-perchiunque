# using nimble file only to generate the lock file
# not a package, not a binary, just a presentation done with nimib and nimislides

# Package

version       = "0.1.0"
author        = "Pietro Peterlongo"
description   = "a presentation with nimislides and nimib"
license       = "MIT"


# Dependencies

requires "nim >= 2.0.8"
requires "nimib >= 0.3.12"